# Changelog

## v0.4.0 - 2026-03-19

### Changed
- 将 QQ 链接/卡片解析前的固定 `/zy` 表情改为随机附带一个 QQ 默认表情，降低重复感。
- 明确插件当前仅支持 AstrBot 的 `aiocqhttp`（OneBot）适配器，并同步收敛 `metadata.yaml` 平台声明与文档说明。

### Added
- 新增媒体缓存清理策略：默认每天自动清理一次缓存；当缓存占用超过 1 GiB 时执行强制清理。
- 新增手动缓存清理命令 `/bili_clear_cache`。

## v0.3.0 - 2026-03-19

### Changed
- 重构配置页分组：将“启用被动自动解析”移入“被动解析设置”，将“监控规则”更名为“主动监控规则”，并整体精简配置标题与说明文字。
- 调整发送策略为双开关模式：`send_direct_video` 控制视频发送，`send_rich_text` 控制图文发送，支持“只发视频”“视频+图文”“只发图文”三种组合。
- 将视频发送大小上限移入“推送与解析内容”，并将“被动解析回复模板”更名为“图文回复模板”。
- 更新 README，使配置页分组、主链路说明和发送策略说明与当前行为保持一致。

### Added
- 新增视频发送时长上限配置，超过指定分钟数时强制改为发送图文。

## v0.2.0 - 2026-03-19

### Changed
- 将“被动识别并解析用户发来的 B 站链接/卡片/BV 号”继续作为插件首要功能，并新增会话白名单/黑名单控制，允许只在指定群聊或用户会话中触发。
- 将视频发送策略调整为默认优先发送视频文件；当下载、合并、体积或环境条件不满足时，自动回退为文本加封面的图文卡片。
- 完善开发文档结构，补充 README 的版本概览、快速开始、配置说明与指令说明。

### Added
- 新增 QQ 被动解析提示表情能力：检测到 `aiocqhttp` 平台中的 B 站链接或分享卡片时，解析消息前追加 QQ `/zy` 表情。
- 新增 `/bili_parse_on` 与 `/bili_parse_off` 指令，用于按当前会话快速开启或关闭被动解析。
- 新增视频下载运行参数：下载超时与最大体积限制。
- 新增 `CHANGELOG.md` 维护版本变更记录。

### Fixed
- 修正订阅推送向多个目标发送时复用同一消息链的潜在兼容性问题，改为按目标逐条构建发送内容。
- 修正默认发视频场景下的缓存媒体大小校验，避免超出体积上限的缓存文件被继续直接发送。

## v0.1.0 - 2026-03-19

### Added
- 初始版本：实现 Bilibili 视频链接/卡片/BV 号被动解析。
- 实现基于 UP 主 UID / 空间链接的轮询订阅推送。
- 实现 Bilibili Cookie 持久化与二维码扫码登录。
- 补齐 AstrBot 插件基础文件：`main.py`、`metadata.yaml`、`_conf_schema.json`、`requirements.txt`、`README.md`。
